<?php
/**
 * Page for slug 'login' — uses Customer Login (Capstone) template.
 * This is the real frontend login page linked from the main navigation.
 */
defined( 'ABSPATH' ) || exit;

require get_stylesheet_directory() . '/template-login.php';

